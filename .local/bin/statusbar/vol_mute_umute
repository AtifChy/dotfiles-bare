#!/usr/bin/env bash

if [ $(pamixer --get-mute) = true  ]; then
	pamixer -u && notify-send "📢 Pamxier" "🔇 Muted"
else
	pamixer -m && notify-send "📢 Pamxier" "🔊 Unmuted"
fi

pkill -RTMIN+7 dwmblocks
