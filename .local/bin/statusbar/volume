#!/usr/bin/env bash

case $BUTTON in
	1) volup ;;
	2) vol_mute_umute ;;
	3) voldown ;;
esac

[ $(pamixer --get-mute) = true ] && echo 🔇 && exit

vol="$(pamixer --get-volume)"

if [ "$vol" -gt "70" ]; then
	icon="🔊"
elif [ "$vol" -lt "30" ]; then
	icon="🔉"
else
	icon="🔈"
fi

echo "$icon $vol%"
