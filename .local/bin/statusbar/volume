#!/bin/sh

case $BUTTON in
	2) if [ $(pamixer --get-mute) = true  ]; then
		pamixer -u && notify-send "📢 Pamxier" "🔇 Muted"
	   else
		pamixer -m && notify-send "📢 Pamxier" "🔊 Unmuted"
	   fi ;;
	4) pamixer --allow-boost -i 1 ;;
	5) pamixer --allow-boost -d 1 ;;
esac

[ $(pamixer --get-mute) = true ] && echo 🔇 && exit

vol="$(pamixer --get-volume)"

if [ "$vol" -gt "70" ]; then
	icon="🔊"
elif [ "$vol" -lt "30" ]; then
	icon="🔉"
else
	icon="🔈"
fi

echo "$icon $vol%"
